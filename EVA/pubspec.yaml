name: eva
description: "Entornos Virtuales de Aprendizaje de la Universidad Guantánamo."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.6
  dio: ^5.7.0
  # url_launcher: ^6.3.1
  # adaptive_dialog: ^2.2.1+2
  # status_bar_control: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  fonts:
    - family: Allerta
      fonts:
        - asset: assets/fonts/Allerta-Regular.ttf
          weight: 400
